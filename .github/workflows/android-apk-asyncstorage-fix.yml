name: Build Android Debug APK (AsyncStorage fix)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 35

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.11.0

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: 8.13

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Pin Async Storage to stable 2.2.0
        run: |
          npm pkg set dependencies."@react-native-async-storage/async-storage"="2.2.0"
          rm -f package-lock.json

      - name: Remove obsolete React Native Gradle flag
        run: |
          sed -i '/^newArchEnabled=/d' android/gradle.properties || true

      - name: Install npm dependencies
        run: npm install

      - name: Accept licenses and install SDK packages
        run: |
          yes | sdkmanager --licenses >/dev/null || true
          sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Build debug APK
        working-directory: android
        run: gradle assembleDebug --no-daemon

      - name: Upload debug APK
        uses: actions/upload-artifact@v4
        with:
          name: quotesnap-debug-apk-asyncstorage-fix
          path: android/app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error
